{:paths ["src"]

 :bbin/bin
 {httpee {:main-opts ["-f" "httpee.bb"]}}

 :tasks
 {test {:doc  "Run tests"
        ;; Run from examples/ so the templates + httpee.edn the tests
        ;; reference resolve relative to cwd, the same way they would for
        ;; an end user invoking `httpee` from their own project.
        :task (shell {:dir "examples"} "bb" "../httpee_test.bb")}}}
